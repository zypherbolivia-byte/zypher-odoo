{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "odoo --db_host=${PGHOST} --db_port=${PGPORT} --db_user=${PGUSER} --db_password=${PGPASSWORD} --http-port=${PORT}"
  }
}
